package MeaningfulNames;
import java.util.*;

public class AvoidDisinformation {
    // DON'T DO THIS
    String[] accountList; // Not actually a list
    String XYZControllerForEfficientHandlingOfStrings; // Takes too long to read
    String XYZControllerForEfficientStorageOfStrings;  // Takes too long to differentiate from the first

    /* Can't tell the difference between the symbols
    int a = l;
    if ( O == l )
        a = O1;
    else
        l = 01;
    */
}
